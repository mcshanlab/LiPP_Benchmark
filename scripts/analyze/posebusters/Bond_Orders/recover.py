'''
Bond order recovery helper adapted from Pat Walters' blog post
"Assigning Bond Orders to PDB Ligands" (https://practicalcheminformatics.blogspot.com/2018/09/assigning-bond-orders-to-pdb-ligands.html).
'''

import sys
import os 
import argparse
from prody import *
import pandas as pd
from rdkit import Chem
from rdkit.Chem import AllChem
from io import StringIO
import requests
import Bio



def read_ligand_expo():
    """
    Read Ligand Expo data, try to find a file called
    Components-smiles-stereo-oe.smi in the current directory.
    If you can't find the file, grab it from the RCSB
    :return: Ligand Expo as a dictionary with ligand id as the key
    """
    file_name = "Components-smiles-stereo-oe.smi"
    try:
        df = pd.read_csv(file_name, sep="\t",
                         header=None, 
                         names=["SMILES", "ID", "Name"])
    except FileNotFoundError:
        url = f"http://ligand-expo.rcsb.org/dictionaries/{file_name}"
        print(url)
        r = requests.get(url, allow_redirects=True)
        open('Components-smiles-stereo-oe.smi', 'wb').write(r.content)
        df = pd.read_csv(file_name, sep="\t",
                         header=None, 
                         names=["SMILES", "ID", "Name"])
    df.set_index("ID", inplace=True)
    return df.to_dict()


def get_pdb_components(pdb_id):
    """
    Split a protein-ligand pdb into protein and ligand components
    :param pdb_id:
    :return:
    """
    pdb = parsePDB(pdb_id)
    protein = pdb.select('protein')
    ligand = pdb.select('not protein and not water')
    print('ligand: from get_pdb_components')
    print(ligand)
    return protein, ligand




def get_pdb_components_file(pdb_file):
    """
    Modified version to parse ligand pdb
    :param pdb_id: pdb file path of the ligand
    :return:
    """
    ligand = parsePDB(pdb_file)
    #ligand = pdb.select('not protein and not water')
    #print('ligand: from get_pdb_components')
    #print(ligand)
    return ligand



def process_ligand(ligand, res_name, expo_dict):
    """
    Add bond orders to a pdb ligand
    1. Select the ligand component with name "res_name"
    2. Get the corresponding SMILES from the Ligand Expo dictionary
    3. Create a template molecule from the SMILES in step 2
    4. Write the PDB file to a stream
    5. Read the stream into an RDKit molecule
    6. Assign the bond orders from the template from step 3
    :param ligand: ligand as generated by prody
    :param res_name: residue name of ligand to extract
    :param expo_dict: dictionary with LigandExpo
    :return: molecule with bond orders assigned
    """
    output = StringIO()
    sub_mol = ligand.select(f"resname {res_name}")
    sub_smiles = expo_dict['SMILES'][res_name]
    template = AllChem.MolFromSmiles(sub_smiles)
    writePDBStream(output, sub_mol)
    pdb_string = output.getvalue()
    rd_mol = AllChem.MolFromPDBBlock(pdb_string)
    new_mol = AllChem.AssignBondOrdersFromTemplate(template, rd_mol)
    return new_mol


def write_pdb(protein, pdb_name):
    """
    Write a prody protein to a pdb file
    :param protein: protein object from prody
    :param pdb_name: base name for the pdb file
    :return: None
    """
    output_pdb_name = f"{pdb_name}_protein.pdb"
    writePDB(f"{output_pdb_name}", protein)
    print(f"wrote {output_pdb_name}")

def write_sdf(new_mol, pdb_file, res_name, out_path, BDID):
    """
    Write an RDKit molecule to an SD file
    :param new_mol:
    :param pdb_name:
    :param res_name:
    :return:
    """
    #base, _ = os.path.splitext(pdb_file)
    # base = os.path.basename(pdb_file)
    # base, _ = os.path.splitext(base)
    # print(f'base: {base}')
    outfile_name = f'{BDID}_alignLipid.sdf'
    #outfile_name = f"{pdb_name}_{res_name}_ligand.sdf"
    writer = Chem.SDWriter(f"{out_path}/{outfile_name}")
    writer.write(new_mol)
    print(f"wrote {outfile_name}")





if __name__ == "__main__":
    parser = argparse.ArgumentParser(description='Process ligand PDB files and assign bond orders')
    parser.add_argument('--output_pdb_dir', type=str, help='Output directory of PDB files to be processed')
    parser.add_argument('--output_sdf_dir', type=str, help='Output directory of SDF files after processing')

    args = parser.parse_args()
    output_pdb_path = args.output_pdb_dir
    output_sdf_path = args.output_sdf_dir
    df_dict = read_ligand_expo()

    
    # Loop over the unique dolphinbuster set
    dolphinbuster_unique_df = pd.read_csv('../Evaluation/unique_rmsd.csv')
    unique_BDIDs = dolphinbuster_unique_df['BioDolphinID'].tolist()

    
    for BDID in unique_BDIDs:
        print(f'processing: {BDID}')

        # Setup directories for processed lipid files
        out_path = output_sdf_path
        os.makedirs(out_path, exist_ok=True)

        # Get the PDB file path for the output lipids
        if os.path.exists(f'{output_pdb_path}/{BDID}_alignLipid.pdb'):
            pdb_file = f'{output_pdb_path}/{BDID}_alignLipid.pdb'
        elif os.path.exists(f'{output_pdb_path}/{BDID}/rank1.sdf'):
            pdb_file = f'{output_pdb_path}/{BDID}/rank1.sdf'
        elif os.path.exists(f'{output_pdb_path}/{BDID}/lipid.pdb'):
            pdb_file = f'{output_pdb_path}/{BDID}/lipid.pdb'
        else:
            print(f'PDB file for {BDID} does not exist. Check directory specifications.')

        # Process the lipid files
        ligand = get_pdb_components_file(pdb_file)
        res_name = BDID.split('-')[-1][:3]
        print(f'res_name: {res_name}')
        print(f'ligand: {ligand}')
        try:
            new_mol = process_ligand(ligand, res_name, df_dict)
            print(f'new_mol: {new_mol}')
            write_sdf(new_mol, pdb_file, res_name, out_path, BDID)
        except Exception as e:
            print(f'Error processing {BDID}: {e}')
    print(f'Finished processing')
